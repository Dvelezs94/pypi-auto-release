name: Publish Python 🐍 distributions 📦 to PyPI
on: push
jobs:
  build-n-publish:
    name: Build and publish Python 🐍 distributions 📦 to PyPI
    runs-on: ubuntu-latest
    steps:
    - name: Bump version and publish release action
      uses: MeilCli/bump-release-action@v1.5.0
